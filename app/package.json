{
  "name": "benchmark-registry-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrations:list:staging": "wrangler d1 migrations list DB --env staging --remote",
    "db:migrate:staging": "wrangler d1 migrations apply DB --env staging --remote",
    "build:staging": "tsc --noEmit && CLOUDFLARE_ENV=staging vite build",
    "verify:staging": "node scripts/verify-staging.mjs",
    "deploy:staging": "tsc --noEmit && CLOUDFLARE_ENV=staging vite build && node scripts/verify-staging.mjs && wrangler deploy --config dist/benchmark_registry/wrangler.json",
    "db:migrations:list:production": "wrangler d1 migrations list DB --env production --remote",
    "db:migrate:production": "wrangler d1 migrations apply DB --env production --remote",
    "build:production": "tsc --noEmit && CLOUDFLARE_ENV=production vite build",
    "verify:production": "node scripts/verify-production.mjs",
    "deploy:production": "tsc --noEmit && CLOUDFLARE_ENV=production vite build && node scripts/verify-production.mjs && wrangler deploy --config dist/benchmark_registry/wrangler.json"
  },
  "engines": {
    "node": "^22.12.0 || ^24.0.0 || >=26.0.0"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.3.0",
    "@fontsource-variable/geist-mono": "^5.3.0",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.54.11",
    "@cloudflare/workers-types": "5.20260917.1",
    "@eslint/js": "10.0.1",
    "@types/react": "19.3.0",
    "@types/react-dom": "19.3.0",
    "@vitejs/plugin-react": "6.1.1",
    "eslint": "10.10.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "eslint-plugin-react-refresh": "0.5.7",
    "globals": "17.12.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.3.0",
    "vitest": "5.0.1",
    "wrangler": "4.133.0"
  }
}
